# Weighted Yield Scores 技術資料

[English](TECHNICAL_NOTES.md) | [READMEへ戻る](README_JA.md)

Weighted Yield Scoresは、Civilization VIIのタイル改善・専門家配置・建造物を、プレイヤーが設定した交換レートに基づく重み付き価値で比較するUI MODです。

## 動的な食料評価

食料は、配置人口（郊外人口と専門家の合計）、現在の食料、次の人口増加に必要な食料、残り評価ターン、上位候補の産出を使い、最大3回の人口増加を概算します。評価期間は最大60ターンですが、ゲーム終了まで60ターン未満の場合は残りターンまでです。市街人口は配置人口へ含めません。

ゲームから取得した現在の必要食料と基礎成長曲線の比率を、将来の必要食料にも適用します。このため、ゲーム速度や成長補正は設定名を判定せず、実際の必要食料を通して反映されます。

`ui/config.js` の `foodValuation` には次の調整値があります。

- `candidateCount`：将来の市民価値に使う上位候補数（初期値3）
- `horizonTurns`：評価する最長期間（初期値60ターン。ゲーム終了まで60ターン未満の場合は残りターンまで）
- `maximumGrowths`：概算する人口増加回数（初期値3）
- `minimumWeight` / `maximumWeight`：食料1の生産力換算値の下限・上限

食料1を生産力1として扱う基準点は、次の人口増加に必要な食料が約315のときです。実際の評価は、現在の人口増加コスト、市民候補の価値、評価期間に応じて変化します。設定で「固定」へ切り替えた場合は、指定した食料倍率を常に使います。

## 動的な幸福度不足補正

幸福度が0以上の範囲では、幸福度1を生産力0.2として評価します。候補によって居住地の幸福度がマイナスになる、またはマイナスから回復する場合は、食料以外の居住地産出が幸福度1につき5%、最大80%変化する価値を「幸福度不足補正」として加減します。

例えば、現在の幸福度が-3で候補から幸福度+5を得る場合、通常価値`5 × 0.2`に加え、-15%から0%へ戻る居住地産出の価値を加算します。0を越えた残り2点には不足補正を加えません。

幸福度20・40の段階に依存する政府能力は評価対象に含めません。

## 資源タイルの評価

資源候補では、タイル固有の産出に加え、獲得直後に未配置となる資源1個の産出を最低価値として加算します。この値はゲーム本体の `getUnassignedResourceYieldBonus` から取得するため、時代と現在のルール補正を反映します。

未配置ボーナスがない帝国資源・宝物資源は対象外です。資源スロットへ配置した場合の固有効果は候補ごとの差が大きいため、現在は加算しません。

## 改善候補の精度

専門家候補は、ゲームが全候補について公開する現在値・配置後・維持費を使うため、候補一覧でも正確です。産出差分は次の式で求め、食料や幸福度の追加維持費もマイナスとして含めます。

```text
(配置後の産出 - 現在の産出) + (現在の維持費 - 配置後の維持費)
```

改善候補の一覧では、現在の地形・特徴・資源の産出と、自動選択される改善の静的な基本産出を合算した推定値を使います。政策、文明能力、倉庫、条件付き隣接などの動的補正は、候補をホバーするまで完全に取得できない場合があります。ホバー後の詳細では、ゲームが返した候補タイルの完全な産出を直接評価します。

## 建造物評価

建造物ごとにゲーム本体の全配置候補を走査し、隣接ボーナス、倉庫効果、既存施設の上書き、維持費を含む産出差分を評価します。

都市建造物で郊外施設を上書きする場合は、消える郊外タイルの総産出を差し引き、移動する人口を現在の盤面で最も価値の高い合法候補へ置いた価値を加算します。建設時は、未完成の建造物自身が持つ専門家枠を候補へ含めません。

固有施設などで既存の改良施設を別の改良施設へ建て替える場合、人口と下にある通常施設はそのタイルに残るため、再配置価値は加算しません。旧施設自身の直接産出の消失には、ゲーム本体の配置差分を使います。

カバカの湖を上書きする場合は、ゲーム本体の配置差分に含まれない湖状態由来の隣接産出を `MapPlotYields` から取得し、実測値を追加で差し引きます。

都市建造物を2棟以上同時に上書きする場合は、区域APIから実際に上書き可能な建造物を取得し、ゲーム側で計算されていない建造物の固定産出の消失と維持費の消滅を追加します。壁など上書き後も残る建造物は対象外です。

通常の建造物画面で未所有の郊外候補を取得できない場合は、居住地の所有境界に隣接する未所有・空きタイルを再構成し、`District_FreeConstructibles` から標準改善を推定します。この推定には地形産出、標準改善の固定産出、資源の最低価値を使います。将来の倉庫・隣接・一部の条件補正までは完全には予測しません。

購入前に存在しない新しい専門家枠について、ゲームのUI APIは専門家1人分の仮想産出を返しません。購入時も現在存在する再配置候補だけを使うため、新設枠が最良になる場合は購入配置を過小評価します。建設の場合は人口再配置が完成前に起こるため、この制限は実際の進行と一致します。

## 建造物リストの共通効率指数

建設・購入の両タブで次の式を使います。

```text
共通効率指数
= 四捨五入（補正後重み付き価値 ÷ 残り生産コスト × 10,000）
```

10,000は小さな小数を読みやすい整数にする表示倍率で、順位には影響しません。購入価格や現在のターン産出は式へ入れないため、同じ時点の同じ建造物は建設タブと購入タブで同じ値・同じ順序になります。これは実際のゴールド効率ではなく、両タブを同じ基準で比較するための指数です。

## 設定

「オプション」→「アドオン」→「重み付き産出スコアの設定」から専用設定画面を開きます。

- 食料の評価方式：動的／固定を切り替えます。動的の間は固定食料倍率の入力欄が無効になります。
- 各倍率：0～3の数値を入力できます。
- 各行の「初期値に戻す」：その産出だけを初期値へ戻します。
- 「すべての設定を初期値に戻す」：食料方式と全産出倍率を初期状態へ戻します。

設定は保存され、全ての評価へ共通して反映されます。ゲーム中に変更した場合は、評価画面をいったん閉じて開き直してください。

## 表示設定

`ui/config.js` の `display` で次の項目を変更できます。

- `showMapScores`：マップ上の候補スコア
- `showHoverPanelScore`：左側パネルのスコア
- `showVersionInPanel`：左側スコアへのMOD版番号表示
- `mapDecimals` / `panelDecimals`：表示する小数桁
- `specialistPipTextOffset`：専門家候補の数字位置
- `specialistMinimumMapFontSize`：専門家候補の最小文字サイズ
- `improvementBadgeScale` / `buildingBadgeScale`：候補バッジの大きさ
- `improvementPosition` / `buildingPosition`：候補表示の位置補正

## 他のUI MODとの関係

他のUI MODとの互換性は未検証です。

表示に問題がある場合は、原因を切り分けるため他のUI MODを一時的に無効化し、単体確認後に1つずつ戻してください。

同じMOD IDの旧版が複数あると読み込まれた版を判別しにくいため、旧版フォルダは残さないでください。

## 開発と診断

自動テストにはNode.jsが必要です。

```bash
npm test
```

診断ログは初期設定で有効です。ゲーム終了後に `UI.log` と `Modding.log` を確認してください。このMODのログ行は `[Weighted Yield Scores]` で始まります。ログが不要な場合は、`ui/config.js` の `diagnostics` を `false` にすると無効化できます。
